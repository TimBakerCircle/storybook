# Vision

Exemplarium is a reverse-Storybook workbench. It starts from an existing app,
builds a semantic UI graph, and helps the user move toward a cleaner design
system, route structure, component model, and Tailwind token language.

The core product is not a story runner. Storybook compatibility is a migration
path and adapter concern, not the center of the architecture.
