# Tailwind Tools

Use `tailwind-merge` for conflict-aware checks. Study Tailwind's official
Prettier plugin for canonical ordering behavior, but do not add formatting
dependencies until cleanup codemods need them.
