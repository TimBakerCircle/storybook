# Agent Frameworks

OpenAI Responses is the P0 provider shape. The OpenAI Agents SDK is a good
future runtime for tool and handoff orchestration. Vercel AI SDK is strongest
for streaming UI. Mastra and LangGraph are later options if workflows need
durable graph execution.
